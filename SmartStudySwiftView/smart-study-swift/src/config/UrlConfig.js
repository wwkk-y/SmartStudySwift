const baseUrl = "http://localhost:538"

const umsBaseUrl = `${baseUrl}/ums`
const fileBaseUrl = `${baseUrl}/file`
const cmsBaseUrl = `${baseUrl}/cms`
const kaBaseUrl = `${baseUrl}/ka`

export default baseUrl;

export {
    baseUrl,
    umsBaseUrl,
    fileBaseUrl,
    cmsBaseUrl,
    kaBaseUrl
}